<template name="otherMessage">
    <view class="message other-message">
        <image src="{{ avatar }}" class="chatAvatar"></image>
        <text class="chatText other-text">{{ message }}</text>
    </view>
</template>